change IsMe and IsDead:
    both should replace the playercard, instead of putting an overlay. no problem with weird edges.
    
    opt 2
    place a full playercard overlay.
    export without drop shadow! adjust pos accordingly ___!!! produces weird blur
    
    option 1 to be preferred update:


    2
    add white rectangle between ult stars. if ult star 1 is active: activate bar between 1 and 2 with it, etc